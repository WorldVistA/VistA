* Finish logging for commands
* Setup logrotate for *.log files
* Figure out $instance for all scripts so more than 1 instance can be on a box
  with same scripts
* Move init script to upstart
    * Ubuntu and RHEL based distros have moved to upstart and consider sysv
      style init scripts legacy
